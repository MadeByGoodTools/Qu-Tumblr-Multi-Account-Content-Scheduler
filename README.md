# Qu authorization callback

This GitHub Pages site provides the OAuth callback page used when connecting a Tumblr account to Qu.

It does not store, transmit, or log the callback address. The page only displays the current address in the browser so it can be copied back into Qu.
